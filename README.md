# lab_implement-and-manage-nginx

Each challenge has a directory with the config files as of the end of the challenge:

- Drop any of these files into `/etc/nginx/conf.d/`
- and then `sudo nginx -s reload`
